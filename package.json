{
  "name": "threejs-vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "capture": "node scripts/capture-game.mjs",
    "capture:compile": "node scripts/capture-game.mjs --compile"
  },
  "devDependencies": {
    "@types/three": "^0.184.0",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "three": "^0.184.0"
  }
}
